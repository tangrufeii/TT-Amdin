/**
 * 领域服务包
 * <p>
 * 职责：
 * 1. 处理跨聚合的复杂业务逻辑
 * 2. 协调多个聚合根之间的交互
 * 3. 封装不属于任何单一聚合根的业务规则
 * 4. 提供高层次的业务操作接口
 * <p>
 * 特点：
 * - 无状态服务，不保存业务状态
 * - 依赖于Repository接口，不依赖具体实现
 * - 可以注入其他领域服务
 * - 可以发布领域事件
 * <p>
 * 与应用服务的区别：
 * - 应用服务：负责流程编排、事务控制、参数验证
 * - 领域服务：负责业务逻辑、规则验证、聚合协调
 * <p>
 * 示例场景：
 * - 跨聚合的转账业务
 * - 复杂的业务规则计算
 * - 需要多个聚合协作的操作
 */
package com.tt.domain.auth.user.service;